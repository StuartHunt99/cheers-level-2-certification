import { FormatSeparatorPipe } from './format-separator.pipe';

describe('FormatSeparatorPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatSeparatorPipe();
    expect(pipe).toBeTruthy();
  });
});
