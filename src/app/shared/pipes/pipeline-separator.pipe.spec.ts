import { PipelineSeparatorPipe } from './pipeline-separator.pipe';

describe('PipelineSeparatorPipe', () => {
  it('create an instance', () => {
    const pipe = new PipelineSeparatorPipe();
    expect(pipe).toBeTruthy();
  });
});
